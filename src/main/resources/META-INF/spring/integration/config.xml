<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/integration"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
			http://www.springframework.org/schema/beans/spring-beans.xsd
			http://www.springframework.org/schema/integration
			http://www.springframework.org/schema/integration/spring-integration.xsd">

    <poller default="true" fixed-rate="100" />

    <channel id="inputChannelHigh" >
        <queue capacity="10"/>
    </channel>

    <header-enricher id="setHigh" input-channel="inputChannelHigh" output-channel="inputChannel">
       <priority value="10"/>
    </header-enricher>

    <channel id="inputChannelLow" >
        <priority-queue capacity="10"/>
    </channel>

    <header-enricher id="setLow" input-channel="inputChannelLow" output-channel="inputChannel">
       <priority value="1"/>
   </header-enricher>

	<channel id="inputChannel">
        <priority-queue capacity="10"/>
  </channel>

	<channel id="outputChannel">
		<queue capacity="10"/>
	</channel>

	<service-activator input-channel="inputChannel"
	                   output-channel="outputChannel"
	                   ref="helloService"
	                   method="sayHello"/>

	<beans:bean id="helloService" class="com.dnlegge.si.example.helloworld.HelloService"/>

</beans:beans>
